<div class="container-fluid">
  <div class="row-fluid">
    <div class="span12">

      <div class="box bordered">
        <div class="box inner light-gray">
          <div class="box-header">
              <i class="icon-group"></i> Portal - Relatórios de Cursos
            </div>
        </div>

        <div class="row-fluid">
          <div class="span12">
            <div class="padded">
              <div class="section-title">Estagiários</div>
              <div style="height: 190px">
              <%= simple_form_for :filtro_1, :url => "/portal/cursos", :remote => true, :html => {:class => 'span12 form-vertical filtro_relatorio'} do |f| %>
                <div class="control-group string optional filtro__empresa"><label class="string optional control-label" for="filtro_1_empresa">Faculdade curso</label><div class="controls"><input list="empresas" class="string optional span2" id="filtro_1_empresa" name="filtro_1[curso]" size="50" type="text">
                  <datalist id="empresas">
                    <% faculdade = Array.new 
                       Curso.all.each do |curso|
                      %>
                    <option value="<%= "#{curso.faculdade} - #{curso.nome}" %>">
                    <% end %>
                  </datalist></div></div>
                <%= f.input :ano, :collection => ["2009", "2010", "2011", "2012", "2013"], :required => false, :label => "Ano", :prompt => "Selecione", :input_html => {:class => 'span2'}, :wrapper_html => {:class => ''} %>
                <%= f.button :submit, "Gerar Relatório" %>
              <% end %>
              </div>
              <div class="chart" id="chart_div1" style="width: 100%; height: 330px;"></div>
            </div>
          </div>
        </div>

        <div class="row-fluid">
          <div class="span12">
            <div class="padded">
              <div class="section-title">Contratações</div>
              <div style="height: 190px">
              <%= simple_form_for :filtro_2, :url => "/portal/cursos", :remote => true, :html => {:class => 'span12 form-vertical filtro_relatorio'} do |f| %>
                <div class="control-group string optional filtro__empresa"><label class="string optional control-label" for="filtro_2_empresa">Faculdade curso</label><div class="controls"><input list="empresas" class="string optional span2" id="filtro_2_empresa" name="filtro_2[curso]" size="50" type="text">
                  <datalist id="empresas">
                    <% faculdade = Array.new 
                       Curso.all.each do |curso|
                      %>
                    <option value="<%= "#{curso.faculdade} - #{curso.nome}" %>">
                    <% end %>
                  </datalist></div></div>
                <%= f.input :ano, :collection => ["2009", "2010", "2011", "2012", "2013"], :required => false, :label => "Ano", :prompt => "Selecione", :input_html => {:class => 'span2'}, :wrapper_html => {:class => ''} %>
                <%= f.button :submit, "Gerar Relatório" %>
              <% end %>
              </div>
              <div class="chart" id="chart_div2" style="width: 100%; height: 330px;"></div>
            </div>
          </div>
        </div>

        <div class="row-fluid">
          <div class="span12">
            <div class="padded">
              <div class="section-title">Taxa de contratos aceitos</div>
              <div style="height: 190px">
              <%= simple_form_for :filtro_3, :url => "/portal/cursos", :remote => true, :html => {:class => 'span12 form-vertical filtro_relatorio'} do |f| %>
                <div class="control-group string optional filtro__empresa"><label class="string optional control-label" for="filtro_3_empresa">Faculdade curso</label><div class="controls"><input list="empresas" class="string optional span2" id="filtro_3_empresa" name="filtro_3[curso]" size="50" type="text">
                  <datalist id="empresas">
                    <% faculdade = Array.new 
                       Curso.all.each do |curso|
                      %>
                    <option value="<%= "#{curso.faculdade} - #{curso.nome}" %>">
                    <% end %>
                  </datalist></div></div>
                <%= f.input :ano, :collection => ["2009", "2010", "2011", "2012", "2013"], :required => false, :label => "Ano", :prompt => "Selecione", :input_html => {:class => 'span2'}, :wrapper_html => {:class => ''} %>
                <%= f.button :submit, "Gerar Relatório" %>
              <% end %>
              </div>
              <div class="chart" id="chart_div3" style="width: 100%; height: 330px;"></div>
            </div>
          </div>
        </div>

        <div class="row-fluid">
          <div class="span12">
            <div class="padded">
              <div class="section-title">Salário Médio</div>
              <div style="height: 190px">
              <%= simple_form_for :filtro_4, :url => "/portal/cursos", :remote => true, :html => {:class => 'span12 form-vertical filtro_relatorio'} do |f| %>
                <div class="control-group string optional filtro__empresa"><label class="string optional control-label" for="filtro_4_empresa">Faculdade curso</label><div class="controls"><input list="empresas" class="string optional span2" id="filtro_4_empresa" name="filtro_4[curso]" size="50" type="text">
                  <datalist id="empresas">
                    <% faculdade = Array.new 
                       Curso.all.each do |curso|
                      %>
                    <option value="<%= "#{curso.faculdade} - #{curso.nome}" %>">
                    <% end %>
                  </datalist></div></div>
                <%= f.input :ano, :collection => ["2009", "2010", "2011", "2012", "2013"], :required => false, :label => "Ano", :prompt => "Selecione", :input_html => {:class => 'span2'}, :wrapper_html => {:class => ''} %>
                <%= f.button :submit, "Gerar Relatório" %>
              <% end %>
              </div>
              <div class="chart" id="chart_div4" style="width: 100%; height: 330px;"></div>
            </div>
          </div>
        </div>
      
      </div>
      
    </div>
  </div>
</div>