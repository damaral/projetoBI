 
$(document).ready(function(){
   $(".filtro_relatorio").submit(function(){

     $.ajax({
        type: 'GET',
        url: $(this).attr("action"),
        data: $(this).serialize(),
        success: function(data){
        },
        dataType: "script"
      });

      return false;
  });
});

 // Load the Visualization API and the piechart package.
google.load('visualization', '1.0', {'packages':['corechart']});



// Set a callback to run when the Google Visualization API is loaded.
//google.setOnLoadCallback(drawChart);


// Callback that creates and populates a data table, 
// instantiates the pie chart, passes in the data and
// draws it.
function drawChartLine(dados) {

  // Create the data table.
  var data = new google.visualization.DataTable();
  data.addColumn('string', 'Mês');
  data.addColumn('number', 'Indicador');
  data.addRows(dados);
  /*data.addRows([
    ['Jan', 5],
    ['Fev', 19],
    ['Mar', 18], 
    ['Abr', 16],
    ['Mai', 14],
    ['Jun', 21],
    ['Jul', 11],
    ['Ago', 17],
    ['Set', 14],
    ['Out', 12],
    ['Nov', 1],
    ['Dez', 2]
  ]);*/

  // Set chart options
  var options = {
                 'width':800,
                 'height':300};

  // Instantiate and draw our chart, passing in some options.
  var chart = new google.visualization.LineChart(document.getElementById('chart_div'));
  chart.draw(data, options);
}

function drawChartColumn(dados) {
        
  var data = new google.visualization.DataTable();
  data.addColumn('string', 'Mês');
  data.addColumn('number', 'Receita');
  data.addColumn('number', 'Custos Variáveis');
  data.addColumn('number', 'Custos Fixos');
  data.addColumn('number', 'Impostos');
  data.addColumn('number', 'Lucros');
  data.addRows(dados);
          
          /*['Year', 'Sales', 'Expenses'],
          ['2004',  1000,      400],
          ['2005',  1170,      460],
          ['2006',  660,       1120],
          ['2007',  1030,      540]
        ]);*/

        var options = {
          title: 'Company Performance',
          hAxis: {title: 'Year', titleTextStyle: {color: 'red'}}
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('chart_div'));
        chart.draw(data, options);
      }
